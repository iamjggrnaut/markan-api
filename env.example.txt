# ============================================
# Nebula Markan API - Environment Variables
# ============================================
# Скопируйте этот файл в .env и заполните значения

# ============================================
# Application
# ============================================
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:3000

# ============================================
# Database (PostgreSQL)
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=nebula_markan
DB_SSL=false

# ============================================
# Redis
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================
# JWT Authentication
# ============================================
# Сгенерируйте надежный секретный ключ (минимум 32 символа)
# Можно использовать: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=7d

# ============================================
# Encryption
# ============================================
# Ключ для шифрования чувствительных данных (API ключи маркетплейсов)
# Сгенерируйте надежный ключ: openssl rand -base64 32
ENCRYPTION_KEY=your-encryption-key-change-in-production

# ============================================
# SMTP (Email)
# ============================================
# Настройки для отправки email уведомлений
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM="Nebula Markan" <your-email@gmail.com>

# ============================================
# Payment Providers
# ============================================
# Выберите провайдера: 'sbp' или 'yookassa'
PAYMENT_PROVIDER=sbp

# SBP (Система быстрых платежей)
SBP_RECIPIENT_NAME=Nebula Markan
SBP_PHONE_NUMBER=+79991234567

# YooKassa
YOOKASSA_SHOP_ID=your-shop-id
YOOKASSA_SECRET_KEY=your-secret-key
YOOKASSA_RETURN_URL=http://localhost:3000/payment/success

# ============================================
# AI Service
# ============================================
# URL сервиса AI (если используется отдельный сервис)
AI_SERVICE_URL=http://localhost:8000

# ============================================
# Telegram Bot
# ============================================
# Токен Telegram бота для уведомлений (опционально)
TELEGRAM_BOT_TOKEN=

# ============================================
# Monitoring & Logging
# ============================================
# Sentry DSN для отслеживания ошибок (опционально)
SENTRY_DSN=

# Уровень логирования: error, warn, info, debug
LOG_LEVEL=debug

